<div class="labelx">{{label}}: {{value}}</div>
<input type="range"
       ($input)="setVal($element.val())"
       {($value)}="value"
       min="{{min}}" max="{{max}}"
/>
<div class="notes">
  <span class="min">min: {{min}}</span>
  <span class="max">max: {{max}}</span>
</div>
